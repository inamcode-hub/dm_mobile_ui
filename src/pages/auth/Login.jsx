import styled from '@emotion/styled';

const Login = () => {
  return (
    <Wrapper>
      <h1>Login</h1>
    </Wrapper>
  );
};

const Wrapper = styled.div`
  background-color: pink;
`;
export default Login;
